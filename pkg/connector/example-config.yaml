# Displayname template for Google Chat ghost users.
# Variables: .Name (full name), .FirstName, .Email
displayname_template: '{{ or .Name .Email "Unknown user" }} (Google Chat)'
# How many most-recently-active chats to create portals for after login.
initial_chat_sync: 20
# Disable sending Matrix media (images/files/video/audio) to Google Chat.
# Outbound uploads are verified working against Google's live endpoint (the
# upload 500s in https://github.com/mautrix/googlechat/issues/114 are a
# client request-shape bug, not shared by this bridge). Enable this only
# if uploads start failing against your account and you'd rather get an
# immediate, clear rejection than a failed send attempt every time.
disable_outbound_media: false
# Disable downloading the media a link chip points at (a shared GIF, a link
# preview image) and inlining it as an image on Matrix.
#
# Worth knowing before you leave this off: unlike every other download the
# bridge does, that host is chosen by whoever sent the message, so fetching it
# reveals the bridge's IP address and the time the message arrived to a third
# party. The fetch is hardened -- https only, no proxy, no cookies, internal
# and link-local addresses refused, redirects and size capped -- and only runs
# for annotations that actually look like media, not for every link anyone
# pastes. Set this to true if you would rather the bridge never made that
# request.
#
# This never costs you a message either way: the URL is always put in the
# message body, so with this enabled a GIF simply arrives as a link.
disable_inline_url_media: false
# History (message) backfill has no settings here: it is controlled entirely
# by this bridge's standard top-level `backfill:` section (not part of this
# network-specific config fragment) -- see that section's own comments, and
# pkg/connector/backfill.go's defaultFetchMessagesCount doc comment for which
# of its keys actually reaches this connector.
